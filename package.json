{
  "name": "ontech",
  "private": "true",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bootstrap": "lerna bootstrap",
    "app-one:start": "cd packages/app-one && yarn start",
    "app-one:build": "yarn --cwd packages/app-one build",
    "app-two:start": "yarn --cwd packages/app-two start",
    "app-two:build": "yarn --cwd packages/app-two build"
  },
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.20.2",
    "yarn": "^1.22.4"
  }
}